services:
  - type: web
    name: project-bogosort
    runtime: python
    branch: app-feature/third-iteration
    buildCommand: bash build.sh
    startCommand: gunicorn wsgi:app
    envVars:
      - key: FLASK_ENV
        value: production
      - key: SECRET_KEY
        generateValue: true
